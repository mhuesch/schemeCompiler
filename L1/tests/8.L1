(((esi <- 5)
  
  (edx <- 1 < 3)
  (edx *= 2)            ;; Number conversion...
  (edx += 1)
  (eax <- (print edx))

  (edx <- 3 < 1)
  (edx *= 2)            ;; Number conversion...
  (edx += 1)
  (eax <- (print edx))

  (edx <- 5 = esi)
  (edx *= 2)            ;; Number conversion...
  (edx += 1)
  (eax <- (print edx))

  (edx <- esi = 5)
  (edx *= 2)            ;; Number conversion...
  (edx += 1)
  (eax <- (print edx))

  (edx <- 9 <= esi)
  (edx *= 2)            ;; Number conversion...
  (edx += 1)
  (eax <- (print edx))

  (edx <- 11)
  (edx <- edx < esi)
  (edx *= 2)            ;; Number conversion...
  (edx += 1)
  (eax <- (print edx))

  (edx <- 5)
  (edx <- edx <= esi)
  (edx *= 2)            ;; Number conversion...
  (edx += 1)
  (eax <- (print edx))

  (edx <- 11)
  (edx <- edx = esi)
  (edx *= 2)            ;; Number conversion...
  (edx += 1)
  (eax <- (print edx))
  ))